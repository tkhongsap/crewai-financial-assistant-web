[build]
builder = "DOCKERFILE"
buildCommand = "docker build -t app ."

[deploy]
startCommand = "docker run -p ${PORT}:3001 app"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "on_failure"